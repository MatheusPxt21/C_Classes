/* DEU ERRO*/

/*#include <stdio.h>
#include <stdlib.h>

void vetor(int TAM)
{
  float v[TAM], soma = 0.0;
  int i;
  for(i=0;i<TAM;i++)
  {
    printf("Insira um valor para v[%d]:\n", i+1);
    scanf("%f", v[i]);
    soma += v[i];
  }
  for(i=0;i<TAM; i++){
    printf("Vetor[%d] == %f\n", i+1, v[i]);
  }
  printf("Soma == %f", soma);
}

void media( int TAM)
{
  float soma;
  printf("Insira o valor que apareceu como a soma: ");
  scanf("%f", &soma);
  float media = soma/TAM;
  printf("\nMedia == %.3f", media);
}

void main()
{
  int t;
  printf("Insira o tamanho do vetor:\n");
  scanf("%d", &t);

  vetor(t);
  media(t);
}
*/